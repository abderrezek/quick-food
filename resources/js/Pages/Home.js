import React from 'react'
import { Alert } from "react-bootstrap";

const Home = ({ name }) => {
    console.log(name);
    return (
        <div>
            Home Page
        </div>
    );
};

export default Home;