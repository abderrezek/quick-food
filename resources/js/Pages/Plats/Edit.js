import React, { useEffect } from "react";

const Edit = ({ plat }) => {
    // useEffect(() => {
    //     if (plat !== {}) {
    //     }
    // }, [plat]);
            console.log(plat);

    return (
        <div>Edit</div>
    );
};

export default Edit;